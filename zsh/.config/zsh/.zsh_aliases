#!/usr/bin/env zsh

# -----------------------------------------------
# --------------------- ZSH ---------------------
# -----------------------------------------------

# source zshrc file
alias s='source $ZDOTDIR/.zshrc'

alias d='dirs -v'
for index ({1..9}) alias "$index"="cd +${index}"; unset index

# alias zsh-update-plugins="find "$ZDOTDIR/plugins" -type d -exec test -e '{}/.git' ';' -print0 | xargs -I {} -0 git -C {} pull -q"

# copy and move with progress
alias rscp='rsync -aP'
alias rsmv='rsync -aP --remove-source-files'

# -----------------------------------------------
# ------------------ Searching ------------------
# -----------------------------------------------

# grep
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# ripgrep
alias rg='rg --hidden --ignore-case'

# find
alias fd='fd --hidden --follow'

# fzf
alias fzfp='fzf --preview "bat --style=numbers --color=always {}"'

# -----------------------------------------------
# ------------------- System --------------------
# -----------------------------------------------

# easier to read disk
alias df='df -h'     # human-readable sizes
alias free='free -m' # show sizes in MB

# get top process eating memory
alias psmem='ps auxf | sort -nr -k 4 | head -5'

# get top process eating cpu ##
alias pscpu='ps auxf | sort -nr -k 3 | head -5'

# -----------------------------------------------
# --------------- Applications ------------------
# -----------------------------------------------

###################################
# git
###################################

# alias ga='git add --verbose'
# alias gaa='git add --all'
# alias gb='git branch'
# alias gc='git commit'
# alias gcm='git commit -m'
# alias gcon='git config --list'
# alias gch='git checkout'
# alias gd='git diff -w'
# alias gf='git fetch'
# alias gp='git push'
# alias gpl='git pull'
# alias gl='git log --graph --oneline --abbrev-commit --decorate --format=format:"%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)" --all'
# alias gm='git merge'
# alias gr='git reset --hard'
# alias gst='git status'
# alias gsh='git stash'

alias lg='lazygit'

# alias cs='~/cht.sh'

###################################
# listing
###################################

alias ls='ls --color=auto --group-directories-first'
alias lsd='lsd --color=auto --group-directories-first'

# tree
alias tree='tree -C --dirsfirst'

###################################
# other
###################################

# resolve symlinks
alias cd='cd -P'

